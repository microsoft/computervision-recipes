# Unit Test steps
steps:

- bash: |
    echo "##vso[task.prependpath]/data/anaconda/bin"
  displayName: Add Conda to PATH

- bash: |
    conda env create -f classification/environment.yml --verbose
    source activate cv
    conda env list  
    pip install papermill lxml ipywebrtc nteract-scrapbook  # to fix error with Windows CPU target
  displayName: 'Create and activate conda environment'

- bash: |
    source activate cv
    python -m ipykernel install --user --name cv --display-name "cv"
    pytest tests/unit --verbose --junitxml=junit/test-unitttest.xml
  displayName: 'Run Unit tests'

- bash: |
      echo Remove Conda Environment
      conda remove -n cv --all -q --force -y
      conda env list
      echo Done Cleanup
  displayName: 'Cleanup Task'
  condition: always()

- task: PublishTestResults@2
  inputs:
    testResultsFiles: '**/test-unitttest.xml'
    testRunTitle: 'Test results for PyTest'
